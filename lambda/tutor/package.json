{
  "name": "quantum-tutor",
  "version": "1.0.0",
  "description": "Streaming Bedrock-backed lesson tutor for the quantum curriculum (Ask the margin)",
  "type": "module",
  "private": true,
  "main": "index.mjs",
  "files": [
    "index.mjs",
    "tutor-core.mjs",
    "corpus.json"
  ],
  "scripts": {
    "test": "node --test",
    "build:corpus": "node ../../scripts/build_tutor_corpus.mjs",
    "predeploy": "npm run build:corpus && node deploy-check.mjs",
    "deploy": "sam build && sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0"
  }
}
